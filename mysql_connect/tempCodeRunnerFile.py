    # cursor = get_sql_connection().cursor()
    # query = f"select product.{select} from\
    #         product inner join unit on product.unit_id = unit.unit_id\
    #         inner join type on product.type_id = type.type_id;"
    # cursor.execute(query)